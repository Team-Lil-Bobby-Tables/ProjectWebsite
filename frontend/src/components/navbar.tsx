import { AppBar } from "@mui/material";

/**
 * Eveything in the navbar component
 * @returns Navbar component
 */
const Navbar = () => {
  return (
    <AppBar position="static">
      This is the nav bar - probably Home, Artifacts, Time Tracking, potentially
      login button far right for time tracking
    </AppBar>
  );
};

export default Navbar;
